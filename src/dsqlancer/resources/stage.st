//STAGE
//<NAME/>

        count = random(<STAGE_MIN/>, <STAGE_MAX/>+1);
        for (int i=0; i<count; i++){
            double index = Math.random()*<TOTAL_WEIGHT/>;
<STAGE_SERIALIZE_RULE/>
        }
