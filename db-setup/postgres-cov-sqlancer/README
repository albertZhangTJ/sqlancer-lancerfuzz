# Build (5-10 minutes)
docker-compose build

# Start
docker-compose up -d

docker exec postgres-fuzzing-sqlancer coverage-helper.sh collect
docker exec postgres-fuzzing-sqlancer coverage-helper.sh report

docker-compose down        # Stop (keep data)
docker-compose down -v     # Remove everything
